#include <stdio.h>
int main(){
    
    char a,b,c;
    scanf("%c%c%c",&a,&b,&c);
    
    printf("%d%d%d\n",(a>=65 && a<=90)||(a>=97 && a<=122) || (a>=48 && a<=57),(b>=65 && b<=90)||(b>=97 && b<=122) || (b>=48 && b<=57),(c>=65 && c<=90)||(c>=97 && c<=122)|| (c>=48 && c<=57));
    printf("%d%d%d",!((a>=65 && a<=90)||(a>=97 && a<=122)),!((b>=65 && b<=90)||(b>=97 && b<=122)),!((c>=65 && c<=90)||(c>=97 && c<=122)));
    return 0;
    
}